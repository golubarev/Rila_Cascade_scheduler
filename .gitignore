# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
.env

# --- Editor ---
.idea/
.vscode/

# --- Project data (large / generated - keep out of git) ---
# The source Excel workbooks are big binaries; keep them locally, not in git.
*.xlsm
*.xlsx
# The database is generated by the loader; rebuild it locally instead of committing.
data/*.db